package Ejercicios.Tren;

import java.util.Comparator;

public class Deposito {
    // 7) ?????????????? COMO TRAIGO LA LISTA DE OTRA CLASE / MISMO PROBLEMA 3)
   // public Integer conjuntoMasPesado();
        /*return vagones.stream()
                .mapToInt(Comparator.comparing(vagon -> vagon.pesoDelVagon())
                        .max()
                        .get().pesoDelVagon());
    }*/


}
